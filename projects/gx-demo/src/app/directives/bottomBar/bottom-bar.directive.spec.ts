import { BottomBarDirective } from './bottom-bar.directive';

describe('BottomBarDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomBarDirective();
    expect(directive).toBeTruthy();
  });
});
